{
  "name": "@aylith/inspekt-mcp",
  "version": "0.1.0",
  "description": "Inspekt MCP server — exposes the grab queue to agents (Claude Code, Cursor, Codex, Gemini CLI, Antigravity) over stdio",
  "type": "module",
  "main": "./dist/index.js",
  "module": "./dist/index.js",
  "bin": {
    "inspekt-mcp": "./dist/cli.js"
  },
  "exports": {
    ".": {
      "import": "./dist/index.js"
    },
    "./cli": "./dist/cli.js"
  },
  "files": ["dist"],
  "scripts": {
    "build": "tsup",
    "dev": "tsup --watch",
    "test": "vitest run --passWithNoTests",
    "typecheck": "tsc --noEmit",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@aylith/inspekt-daemon": "*",
    "@aylith/inspekt-cli": "*",
    "@modelcontextprotocol/sdk": "^1.29.0",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "tsup": "^8.3.0",
    "typescript": "^7.0.2",
    "vitest": "^3.0.0",
    "@types/node": "^22.0.0"
  },
  "license": "MIT"
}
