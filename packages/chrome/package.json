{
  "name": "@aylith/inspekt-chrome",
  "version": "0.8.2",
  "private": true,
  "description": "Inspekt Chrome extension — global settings and standalone inspector",
  "type": "module",
  "scripts": {
    "build": "tsup && cp -r public/* ../../dist/ && cp src/popup/popup.html ../../dist/popup/ && cp src/options/options.html ../../dist/options/ && cp src/welcome/welcome.html ../../dist/welcome/",
    "dev": "tsup --watch",
    "test": "vitest run --passWithNoTests",
    "fetch-metadata": "node scripts/fetch-editor-metadata.mjs",
    "clean": "rm -rf ../../dist"
  },
  "dependencies": {
    "@aylith/inspekt-core": "*"
  },
  "devDependencies": {
    "@types/chrome": "^0.2.2",
    "node-html-parser": "^9.0.0",
    "tsup": "^8.5.1",
    "typescript": "^7.0.2",
    "vitest": "^4.1.10"
  },
  "license": "MIT"
}
